{Let $\vec F$ be a vector field and let $C_1$ and $C_2$ be any nonintersecting paths except that each starts at point $A$ and ends at point $B$. If \underline{\hskip.5in}$=0$, then $\int_{C_1} \vec F\cdot \vec T\ ds = \int_{C_2} \vec F\cdot \vec T\ ds$.
}
{$\curl \vec F$
}
